import SearchInput from "@/components/CElements/SearchInput";

export const Search = () => {
  return (
    <div className="w-[300px]">
      <SearchInput handleChange={() => {}} delay={500} />
    </div>
  );
};
